{
  "name": "@reelapps/monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "ReelApps monorepo - AI-powered talent acquisition platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}